node_modules
.env
.env.local
.env.development
.env.development.local
/src/generated/prisma
/uploads
/dist

# Junction to hans-native's assets/products, used only to serve product
# images locally in dev. Not real files in this repo — do not commit.
/public/assets/products
